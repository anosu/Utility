# Generated by ModEngineering.
name: build
on:
  push:
  pull_request:
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: build-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ !startsWith(github.ref, 'refs/tags/') }}
jobs:
  build:
    uses: anosu/ModEngineering/.github/workflows/consumer.yml@b99f20c018d5d931e289c5dbf0fa1ea23fef97f9
    with:
      kind: library
      extension: false
