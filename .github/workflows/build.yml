# Generated by ModEngineering.
name: build
on:
  push:
  pull_request:
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: build-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ !startsWith(github.ref, 'refs/tags/') }}
jobs:
  build:
    uses: anosu/ModEngineering/.github/workflows/consumer.yml@50f52fbfd45b1b468d23645ee9753cf31423bfca
    with:
      kind: library
      extension: false
